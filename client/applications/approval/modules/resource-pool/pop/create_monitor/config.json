{
  "title": ["create", "health", "monitor"],
  "fields": [{
    "type": "select",
    "field": "resource_pool",
    "empty_text": {
      "info": "no_resource_pool"
    }
  }, {
    "type": "select",
    "field": "probe_type",
    "empty_text": {
      "info": "loading"
    },
    "data": [{
      "name": "TCP",
      "id": "TCP"
    }, {
      "name": "HTTP",
      "id": "HTTP"
    }]
  }, {
    "type": "input",
    "field": "delay",
    "input_type": "text",
    "tip_info": "delay_tip",
    "required": true

  }, {
    "type": "input",
    "field": "timeout",
    "input_type": "text",
    "tip_info": "timeout_tip",
    "required": true
  }, {
    "type": "input",
    "field": "max_retries",
    "input_type": "text",
    "tip_info": "retries_tip",
    "required": true
  }],
  "btn": {
    "value": "create",
    "type": "create",
    "disabled": true
  }
}
